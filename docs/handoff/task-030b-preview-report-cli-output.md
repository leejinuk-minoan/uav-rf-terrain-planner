# Task 030B Preview Report CLI Output

Adds `--report` and `--output-report PATH` by connecting the existing pure report formatter to synthetic and saved-JSON sources. Existing selector, status, UTF-8, overwrite, and explicit-path policies are preserved. No formatter, schema, table, terrain, GIS, scoring, route, waypoint, or UI behavior changes.

Local focused tests, full pytest, compileall, Ruff, mypy, diff, scope, and artifact checks were reported as passed for the initial implementation. GitHub Actions CI was rechecked on the reviewed PR head and completed successfully.
