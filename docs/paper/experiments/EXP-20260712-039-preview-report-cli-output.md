# EXP-20260712-039 - Preview Report CLI Output

Synthetic and saved-JSON report stdout/file modes were tested for deterministic formatting, one trailing newline, selector conflicts, max-record rejection, formatter errors, output-path failures, overwrite protection, and forced overwrite. Temporary artifacts remained outside repository commits. This is a presentation workflow test, not terrain, RF, coordinate-accuracy, or operational validation.
